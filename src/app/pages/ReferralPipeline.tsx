/**
 * ReferralPipeline Page — Lazy-loaded wrapper for the referral intake pipeline board.
 */
import PipelineBoard from '../components/referral-pipeline/PipelineBoard';

export default function ReferralPipeline() {
  return <PipelineBoard />;
}
