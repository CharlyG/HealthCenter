Create the design system for a best-in-class healthcare operations platform focused on home health and hospice. The design system must support complex enterprise workflows, high data density, accessibility, and fast daily use by clinical, scheduling, QA, billing, and administrative roles.

Define and generate:

Foundations

Color tokens: neutral, primary, success, warning, danger, info, surface, border, text, focus, disabled, selected, hover.

Typography scale for page titles, section headers, card headers, body text, secondary text, compact table text, helper text, and micro labels.

Spacing scale using a strict 4/8-based system.

Radius, borders, shadows, and elevation rules.

Iconography rules for a clean enterprise healthcare product.

Semantic usage rules

Statuses must never rely on color alone.

Use semantic badges, pills, icons, and helper text consistently.

Distinguish quiet informational states from blocking or critical states.

Core layout patterns

App shell with sidebar and top bar.

Workspace page layout.

Sticky patient/admission context header.

Split-view layout with primary content and right-side inspector drawer.

Sticky section headers and sticky action footers for long forms.

Core components

Buttons, inputs, selects, date fields, segmented controls, switches, checkboxes, radios, file upload, signature area.

Data table, compact table, queue card, metric card, status pill, badge, tabs, breadcrumbs, command palette, empty state, skeleton loader, timeline, activity feed, drawer, modal.

Healthcare-specific patterns

Patient summary banner

Admission summary panel

Payer summary panel

Authorization warning panel

EVV status card

QA status badge

Medical Director signature queue item

HOPE/OASIS due tracker

Open shift queue item

Delayed visit alert item

Visual direction

Use a calm, modern, highly professional healthcare operations aesthetic. Avoid consumer-app playfulness. Avoid clutter. Prioritize readability, hierarchy, and speed.

Generate the design system in a way that future screens will reuse these components consistently rather than creating custom one-off patterns.